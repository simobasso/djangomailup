============
Installation
============

At the command line::

    $ easy_install djangomailup

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangomailup
    $ pip install djangomailup
